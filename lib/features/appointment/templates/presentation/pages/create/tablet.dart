import 'package:flutter/material.dart';

class TabletCreateTemplatePage extends StatelessWidget {
  const TabletCreateTemplatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tablet Create Template Page'),
    );
  }
}